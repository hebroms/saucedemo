# Blocked Scenarios

The following scenarios were NOT automated because sufficient evidence was not available in the discovery data. Re-run discovery after the application is accessible to generate these tests.

- **Smoke Test: Login with Valid Credentials**
  Reason: Duplicate scenario title — already included
