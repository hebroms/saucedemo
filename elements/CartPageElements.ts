// Locators específicos para a CartPage
import { Locator } from '@playwright/test';

/**
 * Seletores específicos para a CartPage.
 */
export class CartPageElements {
  // Menu e Navegação
  inventoryMenu: Locator;
  cartItems: Locator;
}